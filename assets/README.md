# assets

Put your images and files here:

- `profile.jpg` — your profile photo (square works best, e.g. 400×400)
- `cv.pdf` — your CV
- `pub1.jpg`, `pub2.jpg`, … — publication thumbnails

These filenames are just defaults; change the paths in `../data.json` to match
whatever you name your files.
